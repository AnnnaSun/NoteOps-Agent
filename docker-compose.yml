services:
  postgres:
    image: postgres:18
    container_name: noteops-postgres
    environment:
      POSTGRES_DB: noteops
      POSTGRES_USER: noteops
      POSTGRES_PASSWORD: noteops
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
